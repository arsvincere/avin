# avin_core зависит от avin_utils::AvinError
и из за этого тянет за собой polars который весит тонну и avin_domain тогда
тоже весит тонну.

Возможное решение - вынести
    CoreError
    DomainError

# Serde for domain types?
Должны ли доменные типы... например таймфрейм, InstrumentId уметь сериализоваться и
десереализоваться?

# enum names in python
UPPER_CASE
Плюсы:
- идиоматично для питона
Минусы:
- obj.name возвращает непонятную хуйню... не связанную с rust никак
-------------------------
Like Rust
Плюсы:
- obj.name возвращает понятное раст имя
Минусы:
- не идиоматично для питона
