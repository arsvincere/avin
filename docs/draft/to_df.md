trait DataFrameExt || ToDataFrame+FromDataFrame для Chart, Bar...
    и не держать их в domain, тогда domain не зависит от polars - clean.
    но они могут сразу в прелюде avin и chart.to_df() работает из коробки
    Может service/polars??? ну как вариант пока...
